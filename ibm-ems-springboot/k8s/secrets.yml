# Template only — populate via kubectl, never commit real values
# kubectl create secret generic ems-secrets \
#   --from-literal=jwt-secret=$(openssl rand -hex 32) \
#   --from-literal=db-url='jdbc:mysql://mysql-svc:3306/emsdb' \
#   --from-literal=db-user='emsuser' \
#   --from-literal=db-password='STRONG_PASSWORD'
apiVersion: v1
kind: Secret
metadata:
  name: ems-secrets
  namespace: default
type: Opaque
stringData:
  jwt-secret: "REPLACE_WITH_REAL_SECRET"
  db-url: "jdbc:mysql://mysql-svc:3306/emsdb"
  db-user: "emsuser"
  db-password: "REPLACE_WITH_REAL_PASSWORD"
